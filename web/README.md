# pottle web

the next.js app. see the [repo readme](../README.md) for what pottle is, and [`.env.example`](.env.example)
for every setting.

```bash
cp .env.example .env.local && npm install && npm run dev
```
