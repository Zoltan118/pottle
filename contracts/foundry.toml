[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.30"
optimizer = true
optimizer_runs = 200
evm_version = "prague"

[fuzz]
runs = 1000

[rpc_endpoints]
arc = "${ARC_RPC_URL}"

[invariant]
runs = 256
depth = 60
fail_on_revert = false
